export interface Parameter {
    id: string;
    key: string;
    defaultValue: string;
    countryValues: Record<string, string>;
    description: string;
    createDate: string;
    updatedAt: string;
}